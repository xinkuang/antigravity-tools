name: Release
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
          - platform: "macos-latest"
            args: "--target universal-apple-darwin"
          - platform: "ubuntu-22.04"
            args: ""
          - platform: "ubuntu-24.04-arm"
            args: ""
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (Linux)
        if: startsWith(matrix.platform, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev patchelf pkg-config libsoup-3.0-dev javascriptcoregtk-4.1 libjavascriptcoregtk-4.1-dev
          sudo apt-get install -y libnm-dev xdg-utils

      - name: Rust setup
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || (contains(matrix.args, 'aarch64') && 'aarch64-unknown-linux-gnu' || '') }}

      - name: Node.js setup
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install frontend dependencies
        run: npm install

      - name: Run Rust tests
        working-directory: ./src-tauri
        run: cargo test --all --all-features


      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Antigravity Tools ${{ github.ref_name }}"
          releaseBody: "See the assets to download this version and install."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
          updaterJsonKeepUniversal: true

  docker-build-amd64:
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' && (github.repository == 'lbjlaq/Antigravity-Manager' || vars.ENABLE_DOCKER_PUSH == 'true')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        if: vars.ENABLE_DOCKER_PUSH == 'true' || github.repository == 'lbjlaq/Antigravity-Manager'
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (AMD64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:latest-amd64
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-amd64
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:latest-amd64
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-amd64
          build-args: |
            USE_MIRROR=false

  docker-build-arm64:
    runs-on: ubuntu-24.04-arm
    if: github.event_name != 'pull_request' && (github.repository == 'lbjlaq/Antigravity-Manager' || vars.ENABLE_DOCKER_PUSH == 'true')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        if: vars.ENABLE_DOCKER_PUSH == 'true' || github.repository == 'lbjlaq/Antigravity-Manager'
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (ARM64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:latest-arm64
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-arm64
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:latest-arm64
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-arm64
          build-args: |
            USE_MIRROR=false

  docker-manifest:
    needs: [docker-build-amd64, docker-build-arm64]
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' && (github.repository == 'lbjlaq/Antigravity-Manager' || vars.ENABLE_DOCKER_PUSH == 'true')
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        if: vars.ENABLE_DOCKER_PUSH == 'true' || github.repository == 'lbjlaq/Antigravity-Manager'
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push manifest
        run: |
          # Try pushing Docker Hub manifest (ignore failure if not logged in)
          docker buildx imagetools create -t ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:latest \
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:latest-amd64 \
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:latest-arm64 || true

          docker buildx imagetools create -t ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:${{ github.ref_name }} \
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-amd64 \
            ${{ secrets.DOCKERHUB_USERNAME || github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-arm64 || true

          # Push GHCR manifest (Main target)
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/antigravity-manager:latest \
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:latest-amd64 \
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:latest-arm64

          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/antigravity-manager:${{ github.ref_name }} \
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-amd64 \
            ghcr.io/${{ github.repository_owner }}/antigravity-manager:${{ github.ref_name }}-arm64
